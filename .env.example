# =============================================================================
# F2F DocView Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# See README.md for detailed documentation

# =============================================================================
# REQUIRED - Application will not work without these
# =============================================================================

# Session Security (generate a random 32+ character string)
NUXT_SESSION_PASSWORD=change-me-to-a-long-random-string-min-32-chars

# Discord OAuth (create app at https://discord.com/developers/applications)
NUXT_OAUTH_DISCORD_CLIENT_ID=your_discord_client_id
NUXT_OAUTH_DISCORD_CLIENT_SECRET=your_discord_client_secret
NUXT_REQUIRED_DISCORD_GUILD_ID=your_discord_server_id

# Discord Bot (for role checking)
NUXT_DISCORD_BOT_TOKEN=your_discord_bot_token
NUXT_DISCORD_EDITOR_ROLE_ID=your_editor_role_id

# Git repository URL (default: https://github.com/Fail2Fail-Studios/una)
NUXT_GIT_REPO_URL=https://github.com/your-org/your-repo

# Application URL (update for production)
NUXT_AUTH_BASE_URL=http://localhost:3000

# =============================================================================
# OPTIONAL - Git Configuration (with sensible defaults)
# =============================================================================

# Git repository path (default: /usr/src/app/una-repo)
# Only override if using a custom setup
# NUXT_GIT_REPO_PATH=/usr/src/app/una-repo

# Git branch to track (default: main)
# NUXT_GIT_BRANCH=main

# Git credentials (required for private repos and push access)
# Generate token at: https://github.com/settings/tokens
# NUXT_GIT_USERNAME=your-github-username
# NUXT_GIT_TOKEN=ghp_your_personal_access_token

# Git commit author information (defaults shown)
# NUXT_GIT_COMMIT_NAME=Docs Bot
# NUXT_GIT_COMMIT_EMAIL=docs@fail2.fail

# Git operation timeout in milliseconds (default: 60000)
# NUXT_GIT_TIMEOUT=60000

# =============================================================================
# OPTIONAL - Admin Configuration
# =============================================================================

# Comma-separated list of Discord user IDs with admin privileges
# NUXT_ADMIN_USER_IDS=123456789012345678,987654321098765432

# =============================================================================
# Production-Specific Settings
# =============================================================================

# For production, update these:
# NUXT_AUTH_BASE_URL=https://yourdomain.com
# NODE_ENV=production

# =============================================================================
# Notes
# =============================================================================
# - All NUXT_* variables are accessible in the runtime config
# - Session password should be 32+ characters for security
# - Discord OAuth redirect URIs must match AUTH_BASE_URL/api/auth/discord
# - Git credentials with 'repo' scope needed for private repositories
# - See docs/ directory for detailed configuration guides
